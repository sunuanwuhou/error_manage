# 结构性问题清单

## 1. 导入链路
- 原文对照仍是“附近片段定位”版本，后续可升级为整份 HTML 精确锚点
- 自动校对建议已上线，但仍偏辅助，不应直接自动改库
- 历史旧数据的 `srcExamSession` 规范化仍需补迁移脚本

## 2. 数据一致性
- “同题判定”当前仍有应用层逻辑，后续建议继续向数据库复合唯一约束收口
- 套卷命名、题号、来源字段仍需要统一口径

## 3. 练习闭环
- 结果页 / 交卷页仍需进一步完整化
- 错题 -> 原题 -> 再练 主链已通，但复盘建议层还偏弱
- 普通练习 / 套卷练习 / 知识点回练 仍需进一步统一心智

## 4. 验收纪律
- 导入页浏览器级 smoke 还要更系统
- 删除试卷 / 重复导入 / 失败态要纳入固定回归清单
